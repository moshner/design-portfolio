/*
 * import React from 'react';
 * import {render} from '@testing-library/react';
 * import App from './App';
 */

/*
 * describe('');
 * test('renders ProjectOverviewCard', () => {
 *     render(<App />);
 *     const ProjectOverviewCard = screen.getByText('Design Futures');
 *     expect(linkElement).toBeInTheDocument();
 * });
 */
